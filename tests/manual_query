// NodeLabel(node_name, label)
// EdgeLabel(edge_name, label)
// NodeProperty(node_name, key, value)
// EdgeProperty(edge_name, key, value)
// Connection(edge_name, node_from_name, node_to_name)
// LabeledConnection(label, edge_name, node_from_name, node_to_name)

NodeProperty(?u, "type", "university"),
NodeLabel(?u, "Organisation"),
LabeledConnection("STUDY_AT", ?_e3, ?p2, ?u),
LabeledConnection("STUDY_AT", ?_e2, ?p1, ?u),
LabeledConnection("WORK_AT", ?_e1, ?p2, ?c),
NodeLabel(?p2, "Person"),
NodeProperty(?c, "type", "company"),
LabeledConnection("WORK_AT", ?_e0, ?p1, ?c),
NodeLabel(?p1, "Person"),
NodeLabel(?c, "Organisation"),
NodeProperty(?p1, "id", ?p1.id),
NodeProperty(?p2, "id", ?p2.id)