// SELECT ?pop ?time
// WHERE {
//     <:Q64 wd:P1082 ?popS ?i> .
//     ?popS :numericValue ?pop .
//     ?i wd:P585q [ :time ?time ] .
// } ORDER BY (?time)
SELECT ?pop.amount, ?time.time
MATCH (Q64)-[?e :P1082]->(?pop),
      (?e)-[:P585]->(?time)
LIMIT 100